@inherits LayoutComponentBase

@Body

@* UI-language changes are handled per page via LocalizedComponentBase: a layout re-render does not
   cascade into @Body, so the refresh subscription lives on the routed page, not here. *@
